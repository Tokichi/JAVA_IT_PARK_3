public class IteratorList implements Iterator {
    public int next(List a) {

    }

    public boolean hasNext(List a) {
        if (iteratorFlag <= a.) {
            return true;
        } else {
            return false;
        }
    }
}
